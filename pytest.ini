[pytest]
# The release gate must stay short: the visual probes are parametrized per
# palette, so the cells fan out across workers. Four is the measured sweet
# spot on this machine — five gains nothing (3:12 vs 3:15), more and the
# concurrent Chromes contend and the pixel evidence degrades.
addopts = -n 4
